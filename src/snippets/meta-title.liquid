{% assign divider = ' - ' %}{{- page_title -}}
{%- if current_tags -%}
{%- assign meta_tags = current_tags | join: ', ' -%}{{ divider }}{{- 'common.meta.tags' | t: tags: meta_tags -}}{%- endif -%}
{%- unless page_title contains shop.name -%}{{ divider }}{{- shop.name -}}{%- endunless -%}
